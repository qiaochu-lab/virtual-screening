# 从零理解这个 Benchmark

这份文档面向**没有药物发现或机器学习背景**的读者，从「虚拟筛选是什么」讲起，
说明这个项目要解决什么问题、怎么做的、得到了哪些结论。

英文的逐任务文档在 [`tasks/`](tasks/)，已知局限在
[`LIMITATIONS.md`](LIMITATIONS.md)，本文是它们的入口和串联。

---

## 目录

- [一、虚拟筛选在解决什么](#一虚拟筛选在解决什么)
- [二、这类模型怎么工作](#二这类模型怎么工作)
- [三、这个项目要干什么](#三这个项目要干什么)
- [四、指标](#四指标)
- [五、十个模型](#五十个模型)
- [六、T1 标准基准上的富集](#六t1--标准基准上的富集)
- [七、T3 时间外推（核心）](#七t3--时间外推核心任务)
- [八、T2 亲和力排序](#八t2--能不能判断结合得多强)
- [九、T5 结论稳不稳](#九t5--结论稳不稳)
- [十、T6 物理方法能补短板吗](#十t6--物理方法能补上短板吗)
- [十一、总结](#十一整个-benchmark-加起来说明了什么)

---

## 一、虚拟筛选在解决什么

### 药物发现的起点

假设要做一个治疗某种癌症的药。研究发现某个叫 EGFR 的蛋白质在癌细胞里过度活跃，
把它「关掉」肿瘤就会萎缩。

蛋白质是一条折叠起来的长链，折叠后表面形成一些**凹槽**，其中一个是它干活的地方
——**活性口袋**（binding pocket）。如果能找到一个小分子，形状和化学性质正好塞进
这个凹槽并卡住，蛋白就没法工作了。那个小分子就是候选药物。

问题于是变成：

> **给定一个蛋白口袋，从几千万个可买到的小分子里，找出那几个能卡进去的。**

### 为什么难

- 可买到的化合物库有 10⁷–10⁹ 个分子
- 真正能结合的可能只有几十到几百个
- 实验测一个分子要几百到几千块、几天时间

大海捞针，捞错代价很高。

### 三条技术路线

**1. 高通量实验筛选（HTS）** — 真的拿机器人一个个测。准，但筛十万个分子要几百万
美金和几个月。

**2. 物理模拟** — 在计算机里模拟分子怎么塞进口袋、算结合有多牢。
- **分子对接（docking）**：尝试成千上万种摆放姿势，用打分函数评估。每个分子几秒
  到几分钟；一千万个分子要几个月机时。
- **自由能微扰（FEP）**：更严格的物理模拟，每个分子几小时到几天 GPU。只能用在最后
  几十个候选上。

**3. 机器学习检索** — 本 benchmark 的对象。不做物理，学一个「像不像」的映射，
**几秒钟排完一千万个分子**。

三条路线是互补的。实际流程通常是：机器学习从千万里选出几千 → 对接筛到几百 →
FEP 筛到几十 → 实验验证。**本项目关心第一步。**

---

## 二、这类模型怎么工作

### 核心花招：把化学问题变成几何问题

计算机不能直接理解「分子」，所以先把它变成一串数字：

```
阿司匹林 → [0.31, -1.24, 0.08, ..., 0.77]   （128 个数）
```

这串数字叫 **embedding（嵌入向量）**，可以理解成 128 维空间里一个点的坐标——就像
平面上一个点用 (x, y) 表示，这里用 128 个数。蛋白口袋同样变成一个 128 维的点。

然后：

> **「这个分子能不能结合这个口袋」→ 变成 →「这两个点离得近不近」**

整个领域就靠这一个转换。

### 双塔架构：为什么能做到千万级

```
  口袋 ──→ [口袋编码器] ──→ 向量 A
                              ↓
                        算 A·B 的相似度
                              ↑
  分子 ──→ [分子编码器] ──→ 向量 B
```

两个编码器完全分开，只在最后算相似度时碰头，这叫「双塔」。

**威力在于**：一千万个分子的向量可以提前算好存起来，算一次永久复用。来一个新口袋
只需要算它一个向量，再和一千万个存好的做点积——一次矩阵乘法，几秒钟。

作为对比，「口袋和分子一起喂进网络、输出一个分数」的架构（**交叉编码器**，精度更
高）要跑一千万次前向传播，几天都跑不完，换个口袋还要重来。

**「大规模虚拟筛选」这几个字本身就要求双塔**，精度上的妥协是为换吞吐量。

### 怎么训练：对比学习

这套方法叫 **CLIP**，最早是 OpenAI 做图文匹配的（判断照片和描述配不配），搬到药物
上就是判断口袋和分子配不配。

每次拿一批已知配对，比如 32 对（口袋，能结合的分子），算出各自向量后两两算相似度，
得到 32×32 的矩阵：

```
              分子1  分子2  分子3  ...
      口袋1  [ 高 ]   低     低
      口袋2    低   [ 高 ]   低
      口袋3    低     低   [ 高 ]
```

**训练目标一句话：把对角线拉高，其余全部压低。**对角线是真实配对（正样本），
非对角线是随机凑的（负样本，叫 **decoy** 诱饵）。

「对比」的意思是：不教模型「这个口袋绑什么」，而是教它「这个口袋配这个分子，
不配那 31 个」。

本项目的十个模型绝大多数是这个框架的变体，区别在于口袋怎么编码（3D 结构 vs 序列）、
分子怎么编码（3D 构象 vs 指纹 vs SMILES）、负样本怎么选、向量放在什么空间里。

---

## 三、这个项目要干什么

### 问题的由来

这几年这类模型出了一批论文，每篇都报告很漂亮的数字——DUD-E 上富集因子动辄 40、50 倍。
有三个理由让人怀疑这些数字能不能当真：

**1. 每篇论文用自己的评测口径。**同一个指标，切在 1% 还是 1.2%、取整用 `ceil` 还是
`round`、跨靶点平均还是合并后算，结果能差百分之几十。论文之间的数字根本不可比。

**2. 标准数据集有已知缺陷。**DUD-E 的诱饵是**人工生成的**——挑物理化学性质（分子量、
logP、电荷）匹配但拓扑不同的分子。模型可以学会「靠拓扑特征区分」这个捷径，而真实
化合物库里根本没有这种便利。

**3. 训练数据和测试数据可能重叠。**这些模型都在公开数据库上训练，标准测试集的蛋白
也在那些数据库里。**你没法确定它是在推理还是在回忆。**

### 项目目的

> 在一套统一的、自己控制的评测口径下，把十个模型放在同一把尺子上重测，并专门设计
> 能戳破上述三个缺陷的测试。

不是为了评谁第一，而是搞清楚：论文数字能不能复现、这类方法的能力边界在哪、遇到
没见过的靶点会怎样、能不能判断结合强度、换个设置结论还成不成立、物理方法能不能补短板。

**硬约束：全部用官方代码 + 官方权重**，不重训、不调参。测的是「拿到别人发布的东西，
在统一口径下能得到什么」。

### 六个任务

| | 问的问题 | 文档 |
|---|---|---|
| **T1** 富集 | 论文数字在统一口径下能复现吗？ | [T1](tasks/T1-enrichment.md) |
| **T2** 亲和力排序 | 能不能判断「结合得多强」？ | [T2](tasks/T2-affinity-ranking.md) |
| **T3** 时间外推 | 遇到训练之后才出现的新靶点会怎样？**（核心）** | [T3](tasks/T3-time-split.md) |
| **T4** 靶点垂钓 | 反着用：给分子找靶点 | **未做** |
| **T5** 结构鲁棒性 | 换结构来源、换口袋定义，结论还成立吗？ | [T5](tasks/T5-structure-robustness.md) |
| **T6** 物理互补 | 物理方法能补上短板吗？ | [T6](tasks/T6-physics.md) |

T3 是重心，T2 和 T5 都是在 T3 建好的数据上追加分析。

---

## 四、指标

### EF@1%（富集因子）——最重要

一个靶点有 10,000 个候选分子，其中 100 个是真活性（占 1%）。模型排序后看**最前面
的 1%（100 个）**里有几个真活性：随机排期望是 1 个，模型排出来有 36 个，
则 **EF@1% = 36 倍**。

**为什么最重要**：真实场景里你只有预算买最前面几百个分子做实验。排在第 5000 位的
分子毫无意义，哪怕排得再合理。**虚筛只关心榜首。**

`EF@0.1%` 更严格，`EF@5%` 更宽松。

### AUROC

衡量**整体**排序质量：随机抽一个活性和一个非活性，模型给活性打分更高的概率。
0.5 = 随机，1.0 = 完美。

**关键区别：AUROC 看整个列表，EF 只看榜首。**一个模型可以 AUROC 很高（活性普遍
排得比非活性靠前）但 EF 很低（最顶端那几十个全错）。**对虚筛而言后者才致命。**
这个区别在后面反复出现。

### BEDROC

介于两者之间，按排名给指数衰减的权重。α=80.5 时约 80% 的权重集中在前 2%。

### Spearman ρ

衡量两个排序的一致程度，−1 到 +1。用在 T2：模型打分的排序 vs 实测结合强度的排序。

### 统一评测层

EF 的截断位置必须**向上取整**（`ceil`）——1,234 个分子的 1% 是 12.34 个，
RDKit 官方实现取 13。类似的口径细节还有若干。

所有模型所有任务都走 [`eval/metrics.py`](eval/metrics.py) 这一套代码（80 个测试
对齐 RDKit）。**这是整个项目能成立的前提**：如果每个模型用自己论文的评测代码，
测出的差异分不清是模型差异还是代码差异。

---

## 五、十个模型

| 模型 | 蛋白侧输入 | 分子侧输入 | 训练数据 |
|---|---|---|---|
| **HypSeek** | 3D 口袋 + 序列，**双曲空间** | 3D 构象 | PocketAffDB |
| **LigUnity-pocket** | 3D 口袋 | 3D 构象 | PocketAffDB |
| **LigUnity-protein** | 氨基酸序列 | 3D 构象 | PocketAffDB |
| **LiTENCLIP** | 3D 口袋 | 3D 构象 | PocketAffDB |
| **DrugCLIP** | 3D 口袋 | 3D 构象 | DrugCLIP 集 |
| **BindCLIP-randneg** | 3D 口袋 | 3D 构象 | DrugCLIP 集 |
| **BindCLIP-hardneg** | 3D 口袋 | 3D 构象 | DrugCLIP 集 |
| **ConGLUDe** | 序列 + 结构图 | 分子图 | 自有 |
| **ConPLex** | 序列（蛋白语言模型） | 分子指纹 | BindingDB |
| **SPRINT** | 结构感知序列 | SMILES | 自有 |

确切的 checkpoint 见 [`MODELS.md`](MODELS.md)。

几点解释：

**3D 口袋 vs 序列。** 3D 口袋把凹槽附近的原子坐标喂进去，模型能「看到」形状；
序列只给氨基酸字母串，形状要靠模型自己推。直觉上 3D 应更好，后面会看到这个直觉
不总是对。

**BindCLIP 的 randneg vs hardneg。** 同一架构，只有负样本策略不同（随机负样本 vs
挑那些看起来很像但实际不结合的「难负样本」）。这是天然的对照实验。

**HypSeek 的双曲空间。** 其他模型把向量放在欧几里得空间（距离按勾股定理算），
HypSeek 放在负曲率的双曲空间——该空间半径 r 的球体积按 e^r 增长，正好匹配蛋白家族
的树状层级（激酶 → 酪氨酸激酶 → EGFR → EGFR 突变体，每层节点数指数增长）。它还额外
加了「蕴含锥」约束，把「口袋 ≈ 分子」这种对称关系升级成「口袋 ⊃ 分子」的包含关系。

### 一个后面很重要的事实

十个模型，训练数据只有两大类：

| 训练集 | 规模 | 谁在用 |
|---|---|---|
| **PocketAffDB** | 2,196 个蛋白 | HypSeek、LigUnity ×2、LiTENCLIP |
| **DrugCLIP 集** | 4,098 个蛋白 | DrugCLIP、BindCLIP ×2 |

两者重叠 881 个蛋白。记住这个分组。

---

## 六、T1 — 标准基准上的富集

### 做了什么

十个模型 × 三个标准基准，全部用统一评测层重算。

- **DUD-E**（102 靶点）—— 最经典。真实活性 + 大量**人工生成**的诱饵
- **DEKOIS**（81 靶点）—— 类似思路，诱饵生成方法不同
- **LIT-PCBA**（15 靶点）—— **最真实**。诱饵不是生成的，是真实做过实验、确认无活性的
  分子。所以最难，数字也最低

### 结果（EF@1%）

完整数据：[`results/T1_main.csv`](results/T1_main.csv) · 图：[`figures/fig2_t1_heatmap.png`](figures/fig2_t1_heatmap.png)

| 模型 | DUD-E | DEKOIS | LIT-PCBA |
|---|---|---|---|
| **HypSeek** | **56.4** | **28.8** | 8.3 |
| LiTENCLIP | 44.0 | 24.0 | 6.6 |
| LigUnity-pocket | 42.6 | 24.6 | 7.3 |
| LigUnity-protein | 36.7 | 27.0 | 6.2 |
| BindCLIP-randneg | 32.8 | 19.4 | 6.4 |
| DrugCLIP | 31.9 | 17.9 | 5.5 |
| BindCLIP-hardneg | 27.6 | 17.6 | 6.2 |
| ConGLUDe | 26.5 | 16.6 | **13.2** |
| ConPLex | 18.7 | 10.5 | 2.2 |
| SPRINT | 4.6 | 3.1 | 9.0 |

### 结论

**1. 论文数字基本能复现。**统一口径下各模型的相对高低和论文一致，没有出现「换个
评测代码就崩」的情况。这本身是正面结果。

**2. DUD-E 上的数字虚高得离谱。**看 HypSeek：DUD-E 上 56.4 倍，LIT-PCBA 上只剩
8.3 倍，**掉了 85%**。三个基准里只有 LIT-PCBA 的诱饵是真实实验验证过的。

> 这个落差直接量化了「人工诱饵有多好糊弄」。

**3. 排名会因基准而变。**ConGLUDe 在 DUD-E 上排第 8（26.5），在 LIT-PCBA 上是
**第一名**（13.2），比 HypSeek 还高 60%。SPRINT 也类似——DUD-E 垫底（4.6），
LIT-PCBA 第二（9.0）。

> **在人工诱饵上排名靠前的模型，可能只是更擅长利用人工诱饵的破绽。**

这对读论文有直接影响：只报 DUD-E 数字的工作，可信度要打折。

---

## 七、T3 — 时间外推（核心任务）

### 为什么要自己造数据集

上面三个基准有同一个根本问题：**你没法确定模型是在推理还是在回忆。**这些模型都在
ChEMBL、BindingDB 上训练，DUD-E 的靶点也在那里面。

**解决办法：用时间。**只用**模型训练截止之后才发表**的数据来测试，模型
**不可能**见过——这是物理保证，不是统计假设。

### 数据集怎么建的

**来源**：ChEMBL 37 + BindingDB 202608，只保留 **2024-12 之后**存入的记录
（所有模型的训练截止都在这之前）。**规模**：218,044 条口袋–配体对。

**关键设计：按难度分四层。**因为「新」有程度之分：

| 层 | 靶点 | 分子骨架 | 靶点数 |
|---|---|---|---|
| **L1** | 训练时见过 | 见过的骨架 | 349 |
| **L2** | 训练时见过 | **新骨架** | 488 |
| **L3** | **没见过** | 但同一蛋白家族见过 | 53 |
| **L4** | **没见过**，家族也没见过 | | 254 |

现实对应：

- **L1** ≈ 已知靶点上找类似物（最容易，商业价值也最低，专利都被占了）
- **L2** ≈ 已知靶点上找**新骨架**（有价值，能绕开专利）
- **L3** ≈ 新靶点但属于熟悉家族（又一个新激酶）
- **L4** ≈ **全新靶点**（最有价值也最难，虚筛真正想解决的场景）

**L1 是对照组。**如果模型在 L1 上都接近随机，说明是我们的流程写错了。所有结论都
建立在「L1 明显高于随机」这个前提上。

**诱饵**：不用人工生成的（就是为了避开 DUD-E 的毛病），用**跨靶点诱饵**——从数据池
里挑那些对差异很大的其他靶点有活性的**真实分子**，按 1:50 混进来。都是真实的、有
活性的药物分子，只是不对这个靶点起作用。比人工诱饵难得多。

**活性定义**：pAff ≥ 6（约 IC50 ≤ 1 μM，业界常用门槛）。

> ⚠️ 因为诱饵口径和公开基准不同，**T3 的绝对数字不能和论文里的数字比**。
> 有意义的只有同一套设置内部的 L1→L4 衰减。

### 结果

完整数据：[`results/T3_main.csv`](results/T3_main.csv) ·
置信区间：[`results/T3_main_ci.csv`](results/T3_main_ci.csv) ·
图：[`figures/fig1_t3_decay.png`](figures/fig1_t3_decay.png)

**EF@1%：**

| 模型 | L1 | L2 | L3 | L4 | 衰减 |
|---|---|---|---|---|---|
| LigUnity-protein | 39.18 | 30.20 | 17.81 | 8.83 | −77% |
| HypSeek | 36.63 | 23.61 | 13.56 | 7.34 | **−80%** |
| LigUnity-pocket | 35.24 | 26.39 | 13.90 | 8.39 | −76% |
| LiTENCLIP | 32.37 | 23.06 | 12.94 | 8.46 | −74% |
| BindCLIP-randneg | 19.12 | 12.86 | 8.21 | 5.68 | −70% |
| DrugCLIP | 18.80 | 12.56 | 6.42 | 6.78 | **−64%** |
| BindCLIP-hardneg | 17.81 | 12.57 | 7.90 | 6.00 | −66% |
| ConGLUDe | 13.63 | 7.75 | 5.36 | 3.87 | −72% |
| ConPLex | 7.66 | 3.80 | 3.24 | 2.04 | −73% |

### 结论 1：绝对性能差 5 倍，但衰减幅度所有人都一样

最好的（39.18）是最差的（7.66）的 5 倍，**但从 L1 掉到 L4，每个模型都掉 64–80%**。

> **这是整个 benchmark 最重要的一句话：泛化衰减是这一整类方法的共同性质，
> 不是某个模型的缺陷。**

换架构解决不了。HypSeek 用了双曲空间，衰减 80%，是所有模型里最厉害的。
ConPLex 用纯序列，也一样衰减。

**实际含义**：在一个**全新靶点**上，最好的模型只能给 **8.8 倍**富集——原本要测
1000 个分子才碰上一个活性的，现在测 114 个能碰上一个。有用，但远不是论文 DUD-E
数字（56 倍）暗示的量级。

### 结论 2：训练数据比架构更能解释性能差异

看 L1 那一列按训练集分组：

| 训练数据 | L1 EF1% |
|---|---|
| **PocketAffDB** 组 | **32–39** |
| **DrugCLIP 集**组 | **17–19** |

**组内挤在一起，组间差一倍。**而组内架构差异其实很大：LigUnity-pocket 用 3D 口袋、
LigUnity-protein 用序列，两种完全不同的蛋白表示，结果 35.24 vs 39.18 很接近；
BindCLIP 两个变体负样本策略完全不同，17.81 vs 19.12 也很接近。

同样的分层在 DUD-E 上也成立（前四名全是 PocketAffDB 组）。两张图都按训练集着色，
就是为了让这件事一眼可见。

> **换训练数据的收益远大于换架构。**

这个发现在本领域不太受欢迎——绝大多数论文的卖点在架构创新上。它同时是个方法论
警告：**模型之间的比较是不干净的**。不能说「HypSeek 比 DrugCLIP 好是因为双曲空间」，
因为它俩连训练数据都不是一份。要拆开需要作者做消融实验，那不是 benchmark 能回答的。

### 结论 3：模型排名会因靶点类别而反转

| | 激酶上 | 其他酶上 |
|---|---|---|
| **ConPLex**（纯序列） | 5.76 | 4.73 |
| **ConGLUDe**（几何） | 2.07 | 1.28 |

两类上的相对关系不同，且都通过了 BH-FDR 多重比较校正。**整体均值会给出一个在具体
类别上错误的结论。**

> **不存在「最好的虚筛模型」，只存在「在你这类靶点上最好的模型」。**

如果你的项目做激酶，就该看激酶那一栏，不是总表。

### 结论 4：AUROC 会掩盖 EF 揭示的问题

L4 上 ConGLUDe vs ConPLex：

| | AUROC | EF@1% |
|---|---|---|
| ConGLUDe | 0.570 | **4.00** |
| ConPLex | 0.549 | **2.16** |
| 显著性 | p=0.37，**不显著** | p=0.018，**显著** |

**用 AUROC 看两个模型没区别，用 EF 看一个是另一个的两倍。**因为 AUROC 衡量整个列表，
EF 只看榜首，而虚筛只在乎榜首。

> **只报 AUROC 的论文，等于没回答虚筛真正关心的问题。**

### 一个额外实验：自己训练一个权重

HypSeek 官方只发布了按「排序能力」挑出的存档点（`_rk`），但它在**虚筛**任务上也是
第一名——一个按排序挑的权重在筛选上也最强，有点反常。所以按它论文的配方自己训了
按**筛选**挑的权重（`_vs`），跑了两个随机种子。

完整对照：[`results/T1_T3_hypseek_seeds.csv`](results/T1_T3_hypseek_seeds.csv) ·
过程：[`MODELS_TRAINING.md`](MODELS_TRAINING.md)

| | AUROC 掉多少 | EF@1% 掉多少 |
|---|---|---|
| T3 四个层 | 只掉 **3.6–5.0%** | 掉 **39–48%** |

> **它区分「能结合 vs 不能结合」几乎和官方权重一样好，丢的是排序最顶端那一小段。**

这再次印证结论 4：AUROC 和 EF 测的是不同的东西，而虚筛只用得上后者。

两个种子之间差异只有 0–3%（在样本量大的评测上），所以这不是训练随机性造成的。
顺带得到一个有用的副产品：**种子差异只在小样本评测上才大**——15 个靶点的 LIT-PCBA
上两个种子差 17.6%，而 310 个靶点的 T3 L1 上只差 0.3%。很多被归因为「模型对种子
敏感」的现象，其实是小基准的噪声。

---

## 八、T2 — 能不能判断「结合得多强」

### 问题

T1 和 T3 问的都是**二分类**：能不能结合？但真实研发还需要**分级**：这批候选里
哪个结合得最强？这决定先合成哪个、先测哪个。**这两件事完全不同。**

### 怎么测

用 **Spearman 相关系数**比对模型打分的排序和实测结合强度的排序。在三套数据上做：

| 数据 | 场景 | 特点 |
|---|---|---|
| **T3 数据** | 命中分类 | 同靶点、**跨骨架**、真实世界数据 |
| **FEP 基准**（16 体系） | 先导优化 | 同靶点、**同系列**（结构非常像的一批） |
| **CASF-2016**（55 靶点） | 标准打分函数基准 | 同靶点、跨骨架、精选晶体结构 |

### 结果

数据：[`results/T2_on_T3.csv`](results/T2_on_T3.csv) ·
[`results/T2_on_FEP.csv`](results/T2_on_FEP.csv)

**T3 数据（L1 层）：**

| 模型 | Spearman |
|---|---|
| HypSeek `_rk` | **+0.260** |
| LigUnity-protein | +0.230 |
| LigUnity-pocket | +0.215 |
| LiTENCLIP | +0.171 |
| ConGLUDe | +0.129 |
| DrugCLIP | +0.091 |
| ConPLex | +0.065 |

**FEP 数据**：LigUnity 约 +0.39~0.40
**CASF**：HypSeek 靶点内排序 +0.549，LigUnity-pocket +0.424

### 为什么 CASF 看起来比 T3 好得多

同一批模型 CASF 上 0.42~0.55，T3 上只有 0.09~0.26，差 3–5 倍。看起来像「模型在
真实世界数据上排序能力崩塌」——**这个解释是错的**。

真正的原因是**取值范围受限（restriction of range）**，一个统计学效应：

数据：[`results/T2_range_restriction.csv`](results/T2_range_restriction.csv) ·
脚本：[`physics/t2_gap.py`](physics/t2_gap.py)

| | 靶点内亲和力标准差 | 极差 | 每靶点配体数中位 |
|---|---|---|---|
| CASF-2016 | **1.576** | 4.06 | 5 |
| T3 L1 | **0.783** | 2.99 | 24 |

**T3 的展布正好窄一半**——因为我们自己的评测集有 `pAff ≥ 6` 的门槛，把弱结合的
那半截切掉了。相关系数在真实值展布被压窄时会**机械性地缩小**，这不是模型的问题，
是统计的必然，而且有标准的校正公式（Thorndike case II）。

按展布比 2.01 校正回去：

| 模型 | T3 实测 | 范围校正后 | CASF 实测 | 解释掉 |
|---|---|---|---|---|
| HypSeek `_rk` | 0.260 | **0.477** | 0.549 | **75%** |
| LigUnity-pocket | 0.215 | **0.406** | 0.424 | **91%** |

> **两套数据并没有对这些模型有分歧，它们分歧的是各自配体集里含多少亲和力差异。**

残差（HypSeek 还差 0.07）可能来自标签质量：CASF 用 PDBbind 精选的 Kd/Ki，T3 是
ChEMBL 上混合的 IC50/Ki/EC50，来自不同实验室不同测法。

> ⚠️ 校正值是「这些模型在 CASF 那种展布下会得到什么」的**估计**，不是测量。引用时
> 报实测的 T3 数字，把校正作为差距的**解释**。

### T2 的结论

> **这些模型确实能排亲和力，但很弱。**校正后约 ρ ≈ 0.4~0.48，排序方向大体对，但远
> 不足以支撑「先合成哪个」这类决策。

**排序能力和富集能力一样会随靶点新颖度衰减**（L1 的 0.09~0.26 到 L4 掉到 0.02~0.10）。

**一个有意思的旁证**：HypSeek 那个按「排序」挑出的权重 `_rk`，在**每一层的排序任务上
都领先**。说明「选哪个存档点」这个看似琐碎的决定影响很大——上游作者按什么指标挑权重，
直接决定了模型擅长什么。

---

## 九、T5 — 结论稳不稳

### 为什么需要这一步

T3 得出「所有模型都衰减 64–80%」这个核心结论，但它建立在一堆具体选择上：口袋怎么
定义（距配体 6 Å 以内的残基）、结构从哪来、用结合了配体的结构还是空结构。

**如果换个选择结论就变了，那结论就不是关于模型的，是关于我们的选择的。**

### 对照 1：结构来源 — 实验结构 vs 预测结构

真实场景里很多新靶点没有晶体结构，只能用 AlphaFold / Boltz-2 这类预测结构。够用吗？

| 模型 | 层 | 实验结构 | 预测结构 | p |
|---|---|---|---|---|
| BindCLIP-hardneg | L3 | 9.00±2.41 | 6.00±1.83 | 0.62 |
| BindCLIP-hardneg | L4 | 6.34±1.34 | 5.79±0.97 | 0.32 |
| BindCLIP-randneg | L3 | 8.33±2.09 | 8.00±2.18 | 0.74 |
| BindCLIP-randneg | L4 | 6.11±1.22 | 5.41±0.99 | 0.28 |

**没有任何一处显著差异。结论：预测结构可以替代实验结构。**这对实际应用是好消息。

> ⚠️ 这不是随机对照实验——一个靶点有没有晶体结构本身就不随机（研究得多的才有）。
> 差异很小且方向一致，支持这个读法，但不构成严格证明。

### 对照 2：口袋阈值 — 最敏感的一环

模型训练时用 6 Å 口袋。我们把**整个数据集**在 4 Å 和 8 Å 重建后重跑三个模型。

数据：[`results/T5_pocket_threshold.csv`](results/T5_pocket_threshold.csv)

| | L1 | L2 | L3 | L4 |
|---|---|---|---|---|
| DrugCLIP 4 Å vs 6 Å | −34% | −44% | −42% | −50% |
| DrugCLIP 8 Å vs 6 Å | −62% | −47% | −65% | −52% |
| BindCLIP-randneg 4/8 Å | −32% / −72% | −46% / −60% | −62% / −75% | −38% / −64% |
| BindCLIP-hardneg 4/8 Å | −31% / −64% | −43% / −56% | −43% / −62% | −52% / −39% |

**12 个格子，6 Å 全胜。**偏离训练时的口袋定义，性能掉 31%~75%。

**这是整个 benchmark 里最强的敏感性。**对比一下：换结构来源完全没影响，换口袋大小
掉一半以上。

原因是口袋大小改变了输入的原子数分布：

| 阈值 | 中位原子数 | 相对 6 Å |
|---|---|---|
| 4 Å | 122 | 0.58× |
| **6 Å（训练用）** | 212 | 1.00× |
| 8 Å | 391 | 1.85× |

模型没见过这种分布就退化了。**说明这类模型对输入的「统计形状」高度依赖，不是真的
在理解几何。**

**一个干扰因素被排除**：8 Å 时有 10.8% 的口袋超出模型 511 原子上限会被随机截断，
所以下降可能是截断而非口袋大小造成。拆开看：

| | 数量 | 6 Å | 8 Å | 衰减 |
|---|---|---|---|---|
| 未超上限（完整口袋） | 2,796 | 12.86 | 5.18 | **−59.7%** |
| 超上限（被随机截断） | 336 | 12.57 | 5.18 | −58.8% |

**几乎一样**，所以 8 Å 的退化是真实的口袋尺度效应。

> **实际含义：任何人复用这类模型，必须严格匹配原作者的口袋定义。**
> 这是最容易踩、代价最大的坑。

### 对照 3：apo 构象 — 最接近真实场景

前两项用的都是 **holo 结构**（已经和配体结合过的），问题是蛋白结合配体后形状会变
（诱导契合），holo 结构里已经「刻」进了配体的形状信息。真实筛选往往从 **apo 结构**
（空结构）开始。

**设计**：把 apo 结构按主链原子叠合到 holo 上，然后**用 holo 配体的位置**在叠合后的
apo 结构上切 6 Å 口袋。口袋位置完全一样，**唯一差异是构象**。

数据：[`results/T5_apo.csv`](results/T5_apo.csv)

| 模型 | EF1% holo → apo | AUROC holo → apo |
|---|---|---|
| DrugCLIP | 7.60 → 6.45（−15%，p=0.53） | 0.704 → 0.627（**−11%，p=0.0007**） |
| BindCLIP-randneg | 7.88 → 8.40（+7%，p=0.88） | 0.650 → 0.622（−4%，p=0.32） |

**诚实的总结**：DrugCLIP 在 apo 上整体排序质量显著下降；BindCLIP 的变化和噪声区分
不开；**EF1% 在 45 个靶点上太吵**（p=0.53 和 0.88），两个方向都支持不了。

所以：**apo 构象在 AUROC 上明确伤到两个模型中的一个，而在实践者真正用的指标（EF）
上，这个 benchmark 分辨不出来。**

> ⚠️ 这些 apo 结构口袋侧链偏差中位数只有 1.08 Å（90 分位 2.61 Å），是**轻到中度的
> 诱导契合，不是塌陷的口袋**。结论只能读成「模型对适度构象变化有一定敏感性」，
> 不能外推到困难的 apo 案例。

### T5 总结

| 换什么 | 影响 |
|---|---|
| 结构来源（实验 vs 预测） | **无影响**（p=0.28~0.74） |
| 口袋阈值（4/6/8 Å） | **巨大**（−31%~−75%，6 Å 12 战全胜） |
| holo vs apo 构象 | **中等且不一致**（一个模型 AUROC 显著掉，EF 分辨不出） |

**T3 的核心结论在所有这些扰动下都成立**——衰减幅度会变，但「L1→L4 大幅衰减」这个
模式不变。

---

## 十、T6 — 物理方法能补上短板吗

### 动机

T2 已证明检索模型排亲和力很弱（校正后 ρ ≈ 0.4~0.48），而物理方法（对接、自由能计算）
**在原理上就是算结合强度的**——输出的是能量，有物理量纲。所以最自然的想法是**级联**：

```
一千万分子 → [检索模型，几秒] → top 几百 → [物理方法，慢但准] → top 几十 → 实验
```

**T6 就是检验这个想法。**

### 第一步：物理方法真的更会排序吗

用 **Boltz-2**（共折叠模型：同时预测蛋白-配体复合物三维结构，并带一个预测亲和力的
输出头）。在 16 个 FEP 体系上，**同样的配体、同样的指标**：

数据：[`results/T6_FEP_boltz.csv`](results/T6_FEP_boltz.csv)

| | Spearman | Kendall τ |
|---|---|---|
| **Boltz-2** | **+0.615** | **0.474** |
| 检索模型们 | +0.28 ~ +0.40 | — |
| 已发表的自由能方法（参考） | — | 0.503 |

**Boltz-2 明显更会排序**，接近专业自由能方法。

**但不是横扫**：16 个体系里检索模型赢了 5 个，而且主要是那些**物理参考方法也输**
的体系。

> **这恰恰是两类方法值得结合的理由，而不是分高下的理由**——它们在不同的体系上犯错。

**结论：检索和物理的差距是量的差距，不是质的差距**（0.4 vs 0.6，不是 0 vs 0.6）。

### 第二步：级联重排有用吗

#### 先算天花板

级联有个硬约束：**活性分子没进 top-N，后面再厉害的重排也救不回来。**

脚本：[`physics/shortlist_recall.py`](physics/shortlist_recall.py)

| | top-50 | top-200 |
|---|---|---|
| **L1**（熟悉靶点） | 64.1% | — |
| **L4**（全新靶点） | **17.5%** | 34.0% |

**在全新靶点上，检索的 top-50 只捞到 17.5% 的活性分子。**重排最多只能在这 17.5%
里重新排列。这本身就是一个重要结论：**级联架构在新靶点上，第一级就丢掉了 82% 的目标。**

#### 实验一：Boltz-2 重排 top-50

跑了三轮覆盖不同的层。数据：[`results/T6_rerank3.csv`](results/T6_rerank3.csv)

| | 检索原序 | Boltz-2 重排 | 排名融合 |
|---|---|---|---|
| **L1**（熟悉靶点）AUROC | **0.806** | 0.720 | 0.822 |
| **L4**（全新靶点）AUROC | 0.446 | **0.523** | — |

- **熟悉靶点**：检索打分在它自己的 top-50 里**是有信息的**（0.806），Boltz-2 重排
  **把它弄差了**（0.720）
- **全新靶点**：检索打分在自己的 top-50 里**已接近随机**（0.446），Boltz-2 稍好
  （0.523）但**没法把真正的活性提到最前面**
- **排名融合**在任何一轮里**都没超过更好的那一边**

#### 实验二：换一个完全无关的物理方法

三轮 Boltz-2 共用同一个打分引擎、同一个候选深度——**只靠单一方法的结论其实是关于
那个方法的结论**。所以换 **smina 对接**重做：经验打分函数，没有学习出来的亲和力头，
不共折叠，和 Boltz-2 没有任何共享代码。同时把候选深度从 50 提到 200（天花板翻倍到 34%）。

数据：[`results/T6_dock.csv`](results/T6_dock.csv) ·
图：[`figures/fig3_t6_physics.png`](figures/fig3_t6_physics.png)

| 排序方式 | P@10 | AUROC |
|---|---|---|
| 检索原序 | **0.411** | **0.755** |
| **smina 对接重排** | **0.167** | **0.527** |
| 排名融合 | 0.367 | 0.687 |

ΔP@10 = −0.244（**p = 0.031**），ΔP@20（**p = 0.039**）。

**对接把列表排得比它拿到的检索顺序还差**，直接掉到接近随机。

#### 一个被质疑、然后被检验的点

合作者提出：Boltz-2 的结构是扩散过程随机采样出来的，我们只采了一次，
万一那次折歪了，基于它的亲和力当然不准——所以这个否定结论可能只是采样不足的假象。

这个质疑合理，所以直接测了。先要把两件常被混淆的事分清：

| 参数 | 作用阶段 | 默认 | 我们的值 |
|---|---|---|---|
| `--diffusion_samples` | 结构预测 | 1 | **1**（后改成 5 重跑） |
| `--diffusion_samples_affinity` | 亲和力预测 | **5** | 5（一直没改） |

**亲和力打分从来就不是单样本**——它自己跑 5 次扩散。只有结构阶段是 1。

而且多采样**不等于 multi-pose 打分**：结构阶段采 N 个后按置信度排名，
**只有第一名会被交给亲和力模型**。所以提高 N 买到的是「best-of-N 选一个更好的
输入结构」，不是「对 N 个 pose 分别打分再聚合」。Boltz-2 根本不接受外部给的
pose——这一点和传统 docking 完全不同。

**检验**：同样 750 个复合物、同样的输入和 MSA，只把 `--diffusion_samples`
从 1 改成 5，逐复合物配对比较（749 个两轮都有分）。

| 排序方式 | P@5 | P@10 | AUROC |
|---|---|---|---|
| 检索基线 | **0.533** | **0.333** | 0.806 |
| Boltz-2 N=1 | 0.333 | 0.225 | 0.720 |
| Boltz-2 **N=5** | 0.317 | 0.233 | **0.718** |

配对检验：**AUROC 只差 0.002，四个指标的 p 值全在 0.9 以上。**

> **结构采样质量不是瓶颈。**给它 5 个结构做筛选，和给它 1 个未经筛选的，
> 打分能力完全一样。这让 T6 的结论更硬，不是更软。

### T6 的结论

> **把检索模型的候选列表交给物理方法重排，在我们测试的所有条件下都没有带来收益。**
> 两个方法——共折叠 + 学习亲和力头，和经典经验打分函数——**没有任何共同点，方向
> 却完全一致。**

必须说清的几点：

1. **这是关于「级联重排」这个用法的结论，不是关于物理方法的结论。**Boltz-2 在
   **独立排序**任务上明确比检索模型强（0.615 vs 0.40），它只是在检索给的候选列表
   **内部**重排时不起作用。
2. **一个重要原因是天花板。**L4 上 top-50 只有 17.5% 召回率，重排在一个已经丢了
   82% 目标的列表里做文章，空间本来就很小。
3. **样本量小。**对接那轮 20 个靶点只有 9 个能算（其余是 top-200 里活性不足 2 个
   ——还是召回天花板）。显著性有，但效应量估计不可靠。
4. **对接是在口袋切片里做的**，盒子来自提取出的口袋的外接框。和检索模型看到的是
   同一个口袋定义，比较公平，但不是一篇对接论文会用的完整设置。

### 一个反直觉的发现

「检索模型的打分在它自己的候选列表内部有没有用」，**取决于靶点熟不熟悉**：

- 熟悉靶点（L1）：**有用**（AUROC 0.806）
- 全新靶点（L4）：**接近随机**（AUROC 0.446）

**只测一层会得出完全错误的结论。**只测 L4 会说「检索打分在候选内部毫无价值」，
只测 L1 会说「很有价值」——都对，都不完整。

---

## 十一、整个 benchmark 加起来说明了什么

### 八条主要结论

**1. 这类方法在新靶点上的能力，比论文数字暗示的低一个量级。**
DUD-E 上 56 倍 → 真实诱饵（LIT-PCBA）8 倍 → 全新靶点（T3 L4）8.8 倍。同一批模型。

**2. 泛化衰减（64–80%）是整个方法家族的共同性质。**
十个模型、多种架构、两套训练数据、欧氏和双曲空间，没有一个例外。换架构解决不了。

**3. 训练数据比架构更能解释性能差异。**
同一份数据训出来的挤在一起，不同数据训出来的差一倍——尽管架构差异远大于此。

**4. 不存在「最好的模型」，只存在「在你这类靶点上最好的模型」。**
排名会因靶点类别反转，也会因基准反转。

**5. AUROC 和 EF 测的是不同的东西，而虚筛只用得上 EF。**
两个模型可以 AUROC 无差异而 EF 差一倍；自训的权重 AUROC 只掉 4% 而 EF 掉 40%。
**只报 AUROC 的工作，没有回答虚筛的问题。**

**6. 排亲和力的能力弱但真实**（校正后 ρ ≈ 0.4~0.48），**且同样随靶点新颖度衰减。**
不足以支撑「先合成哪个」这类决策。

**7. 对口袋定义极度敏感，对结构来源不敏感。**
换预测结构没影响；换口袋阈值掉一半以上。**复用必须严格匹配原作者的口袋定义。**

**8. 物理方法在独立排序上更强，但用它重排检索的候选列表没有收益。**
两个互不相干的物理方法方向一致。主要瓶颈在第一级的召回天花板。

### 对实际工作的含义

**如果你要用这类模型做筛选：**
- 熟悉的靶点家族上能给 13~39 倍富集，很有价值
- 全新靶点上最多 8~9 倍，能用但要调低预期
- 严格照抄原作者的口袋提取参数
- 没有晶体结构不是问题，预测结构够用
- 看模型在**你这类靶点**上的表现，不要看总排名
- 别指望它告诉你哪个结合得更强

**如果你要读这个领域的论文：**
- 只报 DUD-E 数字的，打折
- 只报 AUROC 的，没回答虚筛的问题
- 没有时间切分或类似防泄漏设计的，无法判断是推理还是记忆
- 声称架构创新带来提升的，先看是不是换了训练数据

**如果你要改进这类方法：**
- 衰减是全家族共性，说明瓶颈可能在**范式**而不是架构
- 训练数据的杠杆比架构大
- 级联重排这条路，瓶颈在第一级的召回，不在第二级的打分
- 想用物理补短板，得先解决「活性分子进不了候选列表」的问题

---

## 延伸阅读

| | |
|---|---|
| [`LIMITATIONS.md`](LIMITATIONS.md) | 每一个数字可能出错的已知原因，按对结论的影响排序 |
| [`MODELS.md`](MODELS.md) | 确切的 checkpoint、为什么选这个变体、各模型的接口怪癖 |
| [`MODELS_TRAINING.md`](MODELS_TRAINING.md) | 自己训练 HypSeek 筛选权重的完整经过 |
| [`PATCHES.md`](PATCHES.md) | 第三方代码的改动，以及我们自己代码里的 bug |
| [`results/RAW_SCORES.md`](results/RAW_SCORES.md) | 逐分子原始打分的格式与复算方法 |
| [`figures/`](figures/) | 三张总结图及重建脚本 |
